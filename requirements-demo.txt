# ==============================================================================
# BotV2 - DEMO MODE Requirements (Lightweight)
# ==============================================================================
# Optimized for FAST Docker builds in demo/development mode.
# Removes heavy dependencies not needed for demo:
#   - No PostgreSQL (psycopg2) - uses mock data
#   - No ML libraries (scikit-learn, scipy) - not used in demo
#   - No backtrader - not used in demo dashboard
#   - No TA library - demo uses pre-calculated indicators
#
# BUILD TIME: ~2-3 minutes (vs 10-15 min with full requirements)
#
# USAGE (Docker):
#   Demo mode:  docker-compose -f docker-compose.demo.yml up -d
#   Production: docker-compose -f docker-compose.production.yml up -d
#
# USAGE (Local):
#   pip install -r requirements-demo.txt
#   python run_demo.py
#
# Last updated: 30-01-2026
# ==============================================================================

# ===== CORE CONFIGURATION =====
PyYAML==6.0.1

# ===== DATA PROCESSING (Minimal) =====
# numpy and pandas are needed for dashboard charts
numpy==1.26.4
pandas==2.1.4
# scipy removed - not needed for demo
# scikit-learn removed - not needed for demo

# ===== DATABASE & CACHING =====
# psycopg2-binary removed - demo uses mock data
SQLAlchemy==2.0.23
redis==5.0.1

# ===== TRADING & EXCHANGE APIs =====
requests==2.31.0
websockets==12.0
websocket-client==1.6.4
ccxt==4.0.79
# backtrader removed - not used in demo dashboard

# ===== WEB FRAMEWORK & DASHBOARD =====
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5
Flask-Limiter==3.5.0
Flask-Talisman==1.1.0
Flask-WTF==1.2.1
Flask-Compress==1.14
bleach==6.1.0
dash==2.14.2
plotly==5.18.0
python-socketio==5.10.0
eventlet==0.33.3

# ===== MONITORING =====
prometheus-client==0.19.0
psutil>=5.9.0

# ===== UTILITIES =====
python-dotenv==1.0.0
python-dateutil>=2.8.2
colorlog==6.8.0
tqdm==4.66.1
jsonschema>=4.19.0

# ===== SECURITY =====
cryptography==41.0.7
pydantic==2.5.3
python-jose==3.3.0

# ===== VISUALIZATION (for dashboard charts) =====
matplotlib==3.8.0
seaborn==0.13.0

# ===== TESTING (Minimal) =====
pytest>=7.4.3

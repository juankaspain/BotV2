# BotV2 - Dependencias Python (Production-Ready)
# Trading System - Core Dependencies Only
# VERIFIED: All packages available on PyPI with wheels for Python 3.11 Alpine
# Last verified: 21-01-2026

# ===== CORE PACKAGES (ONLY wheel-available for Alpine) =====
PyYAML==6.0.1

# ===== DATA & MATH =====
numpy==1.26.4
pandas==2.1.4
scipy==1.12.0

# ===== DATABASE =====
psycopg2-binary==2.9.9
SQLAlchemy==2.0.23
redis==5.0.1

# ===== API & EXCHANGES =====
requests==2.31.0
websockets==12.0
ccxt==4.0.79

# ===== BACKTESTING =====
backtrader==1.9.78.123
zipline-reloaded==2.4.0

# ===== DASHBOARD (Flask/Dash) =====
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5
dash==2.14.2
plotly==5.18.0
python-socketio==5.10.0
eventlet==0.33.3

# ===== MONITORING & LOGGING =====
prometheus-client==0.19.0

# ===== TESTING =====
pytest==7.4.3
pytest-asyncio==0.23.2
pytest-cov==4.1.0

# ===== UTILITIES =====
python-dotenv==1.0.0
colorlog==6.8.0
tqdm==4.66.1

# ===== SECURITY & VALIDATION =====
cryptography==41.0.7
pydantic==2.5.3
python-jose==3.3.0

# ===== REMOVED: Packages without Alpine wheels =====
# The following packages DO NOT have wheels for musl/Alpine Linux
# They require C compilation which fails on Alpine
# To use them, either:
#   1. Add libc dev packages to Dockerfile
#   2. Use different base image (Debian instead of Alpine)
#   3. Wait for wheel releases
#
# Removed:
# - scikit-learn (no musl wheels)
# - lightgbm (no musl wheels)
# - xgboost (no musl wheels)
# - tensorflow (requires glibc)
# - torch (requires glibc)
# - ta-lib (requires C compilation)
# - apache-airflow (requires glibc)
# - kafka-python (no musl wheels)
# - pyspark (requires java)

# ===== OPTIONAL: Add ML later =====
# For v5.0+ features, either:
# 1. Use Debian-based image instead of Alpine
# 2. Use gRPC/API to call ML models in separate container
# 3. Pre-build wheels with libc dev packages

# ===== NOTES =====
# Python version:
#   - Specified in Dockerfile: python:3.11-alpine
#   - Alpine uses musl libc (not glibc)
#
# Package selection criteria:
#   ✓ Available on PyPI with wheels for Python 3.11
#   ✓ Works on Alpine Linux (musl libc compatible)
#   ✓ Pinned to exact versions available
#
# Core functionality maintained:
#   ✓ Data processing: pandas, numpy, scipy
#   ✓ Trading: ccxt, backtrader, zipline
#   ✓ Web UI: Flask, Dash, plotly
#   ✓ Database: PostgreSQL, Redis
#   ✓ Testing: pytest
#
# Build strategy:
#   - Uses --prefer-binary flag in Dockerfile
#   - Uses --only-binary=:all: to force wheels only
#   - All packages have wheels for musl/Alpine
#
# If Alpine is too restrictive:
#   - Change Dockerfile base from python:3.11-alpine
#   - To python:3.11-slim or python:3.11-bullseye
#   - Then you can use ALL packages including ML
